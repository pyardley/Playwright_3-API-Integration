# Support

Shared step helpers (steps.ts), test-data builders (testData.ts), and other
utilities used across test files. Functions here should be thin orchestration
wrappers — the actual page interactions live in page objects and components.
